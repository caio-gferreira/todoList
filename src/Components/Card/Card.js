import React from 'react';
import { Header } from '../Header/Header'
import { Input } from '../Input/Input';

export const Card = () => {
    return (
        <div>
            <Header />
            <Input />
        </div>
    )
}